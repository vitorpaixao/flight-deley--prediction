# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

Flight delay prediction project using the 2015 US DOT "Flight Delays and Cancellations" dataset (~5.8M rows). This is an academic project (POS - postgraduate course, Phase 3: Machine Learning Engineering). The project language is Portuguese (Brazilian).

## Development Setup

- **Python 3.13**, managed with **uv** (see `pyproject.toml` and `uv.lock`)
- Virtual environment: `.venv/`
- Install dependencies: `uv sync`
- Install with dev deps (JupyterLab): `uv sync --group dev`
- Run scripts: `uv run python <script.py>` (scripts assume CWD is project root for `data/` paths)
- Launch Jupyter: `uv run jupyter lab`

## Data

### Raw CSVs in `data/`
- `flights.csv` — ~5.8M flight records (2.4 GB in memory). Use `low_memory=False` when reading with pandas.
- `airports.csv` — airport metadata
- `airlines.csv` — airline code lookups

### Cleaned Parquet
- `data/flights_clean.parquet` — ~169MB, generated by `notebooks/limpeza.ipynb`. ~5.7M rows × 41 cols. Includes joins with airlines/airports, cleaned NaNs, feature engineering (DEP_HOUR, SEASON, IS_WEEKEND, IS_DELAYED).

Data dictionary is in `plan/dic_data.md`. Original challenge spec PDF is in `plan/origin/`.

### Key data quirks
- Delay cause columns (`AIR_SYSTEM_DELAY`, `SECURITY_DELAY`, `AIRLINE_DELAY`, `LATE_AIRCRAFT_DELAY`, `WEATHER_DELAY`) are ~82% NaN by design — only populated for delayed flights. Imputed with 0.
- `CANCELLATION_REASON` is ~98.5% NaN (only cancelled flights have a reason). Cancelled flights are removed in cleaning.
- `DEPARTURE_DELAY`/`ARRIVAL_DELAY` ~1.5% truly missing — dropped in cleaning.
- Target variable: `IS_DELAYED = DEPARTURE_DELAY > 15` (binary, ~82/18 split — moderate imbalance).

## Architecture

### Notebooks (main workflow)
```
notebooks/EDA_exploratorio.ipynb     — EDA on raw CSV data (joins, missing values, distributions)
notebooks/limpeza.ipynb              — Cleaning pipeline → exports data/flights_clean.parquet
notebooks/EDA_clean.ipynb            — EDA on cleaned Parquet (correlations, patterns, feature matrix)
notebooks/modelagem_supervisionada.ipynb    — Classification (LR vs XGBoost) + Regression (LinReg vs LightGBM vs XGBoost)
notebooks/modelagem_nao_supervisionada.ipynb — PCA (delay causes) + K-Means (airport profiles)
notebooks/apresentacao.ipynb         — Summary for presentation video
```

### Modules
```
modules/data_loader.py  — load_flights_clean(), build_classification_split(), build_regression_split()
modules/evaluation.py   — print_classification_report(), plot_confusion_matrix(), plot_roc_curves(),
                          print_regression_report(), plot_residuals(), plot_feature_importance()
```

### Legacy (kept as reference, not in main flow)
- `notebooks/eda_flights.ipynb` — original EDA notebook
- `notebooks/fly.ipynb` — extended analysis (incomplete)
- `src/data_analisys/` — standalone scripts
- `main.py` — placeholder

### Other
- `plan/` — project documentation: data dictionary (`dic_data.md`), external data research (`external_data.md`), official requirements (`tech_challenge.md`)

### Modeling pipeline
1. **Cleaning** (`limpeza.ipynb`): joins, drop YEAR/TAIL_NUMBER/FLIGHT_NUMBER, remove cancelled flights, fillna(0) delay causes, dropna(DEPARTURE_DELAY), fillna("Desconhecido") join cols, feature engineering → Parquet
2. **Features**: MONTH, DAY_OF_WEEK, DEP_HOUR, SEASON, IS_WEEKEND, DISTANCE + 14 airline dummies → 20 features
3. **Split**: 80/20 train/test with `np.random.default_rng(42)`
4. **Classification**: Logistic Regression vs XGBoost for IS_DELAYED
5. **Regression**: Linear Regression vs LightGBM vs XGBoost for DEPARTURE_DELAY (delay > 0 only)
6. **Unsupervised**: PCA on delay causes (IS_DELAYED==1 only), K-Means on airport profiles

### Key dependencies (dev group)
scikit-learn, seaborn, xgboost, lightgbm, pyarrow, jupyterlab, ipykernel

### Academic requirements (`plan/tech_challenge.md`)
- **Mandatory**: EDA with missing value treatment, supervised modeling (classification OR regression, compare 2+ algorithms), unsupervised modeling (clustering or dimensionality reduction), critical presentation
- **Optional**: additional feature engineering, geographic analysis, dashboards, anomaly detection
